{
  "name": "ariel",
  "version": "0.1.0",
  "description": "Render Mermaid diagrams in your terminal",
  "type": "module",
  "scripts": {
    "build": "bun build cli.ts --compile --outfile ariel",
    "install-cli": "bun run build && codesign --force --sign - ariel && sudo cp ariel /usr/local/bin/ariel && echo 'Installed ariel to /usr/local/bin/ariel'",
    "dev": "bun run cli.ts"
  },
  "dependencies": {
    "beautiful-mermaid": "^1.1.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "typescript": "^5.9.3"
  }
}
